# frozen_string_literal: true

RSpec.describe LlmMemoryGmailLoader do
  it "has a version number" do
    expect(LlmMemoryGmailLoader::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
